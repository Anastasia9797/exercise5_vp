<Window x:Class="exercise5_vp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:exercise5_vp"
        mc:Ignorable="d"
        Title="MainWindow" Height="425" Width="450">
    <Grid>
        <TabControl Margin="10" BorderThickness="1" Background="LightPink" BorderBrush="DeepPink">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="IndianRed" CornerRadius="4,4,0,0" Margin="2,0">
                                    <ContentPresenter x:Name="ContentSide" VerticalAlignment="Center" 
                                                      HorizontalAlignment="Center" ContentSource="Header"
                                                      Margin="10,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="LightSalmon"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="Bisque"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Загальне">
                <Image Source="C:\Users\User\Pictures\Screenshots\Знімок екрана 2025-04-22 180304.png" Margin="10"/>
            </TabItem>
            <TabItem Header="Безпека">
                <Label Content="Тут міститься якийсь контент"/>
            </TabItem>
            <TabItem Header="Деталі"/>
        </TabControl>
    </Grid>
</Window>
